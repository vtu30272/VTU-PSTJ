class Solution {
    public void moveZeroes(int[] nums) {
        int insertPos = 0;

        // First pass: move all non-zero elements to the front, preserving their order
        for (int num : nums) {
            if (num != 0) {
                nums[insertPos] = num;
                insertPos++;
            }
        }

        // Second pass: fill the remaining positions with zeros
        while (insertPos < nums.length) {
            nums[insertPos] = 0;
            insertPos++;
        }
    }
}
